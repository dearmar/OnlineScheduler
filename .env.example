# ===========================================
# Calendar Scheduler - Environment Variables
# ===========================================

# Application URL (your Vercel deployment URL)
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app

# ===========================================
# Microsoft Azure AD / Graph API
# ===========================================
# Create an app registration at https://portal.azure.com
# Required permissions: Calendars.ReadWrite, Mail.Send, User.Read

MICROSOFT_CLIENT_ID=your-azure-app-client-id
MICROSOFT_CLIENT_SECRET=your-azure-app-client-secret
MICROSOFT_TENANT_ID=common

# OAuth redirect URI (must match Azure AD app registration)
MICROSOFT_REDIRECT_URI=https://your-app.vercel.app/api/auth/callback

# ===========================================
# Vercel KV (Redis) - Data Storage
# ===========================================
# Create a KV store in your Vercel dashboard
# These are auto-populated when you link KV to your project

KV_URL=your-kv-url
KV_REST_API_URL=your-kv-rest-api-url
KV_REST_API_TOKEN=your-kv-rest-api-token
KV_REST_API_READ_ONLY_TOKEN=your-kv-read-only-token

# ===========================================
# Email Service (Resend)
# ===========================================
# Sign up at https://resend.com

RESEND_API_KEY=re_your_api_key
EMAIL_FROM=noreply@yourdomain.com

# ===========================================
# Admin Authentication
# ===========================================
# Generate a secure random string for JWT signing
# You can use: openssl rand -base64 32

JWT_SECRET=your-super-secure-jwt-secret-min-32-chars
ADMIN_EMAIL=admin@yourdomain.com

# Initial admin password (change after first login)
ADMIN_INITIAL_PASSWORD=change-this-password

# ===========================================
# Webhook Configuration (Optional)
# ===========================================
# Secret for validating incoming webhooks

WEBHOOK_SECRET=your-webhook-secret

# External webhook URL to notify on new bookings (optional)
EXTERNAL_WEBHOOK_URL=https://your-service.com/webhook

# ===========================================
# Feature Flags
# ===========================================

ENABLE_EMAIL_NOTIFICATIONS=true
ENABLE_CALENDAR_SYNC=true
ENABLE_WEBHOOKS=true
